// Vitest setup file
// This file is run before each test file

// Global setup for jsdom environment when needed
import { beforeAll } from "vitest";

beforeAll(() => {
	// Setup global variables or configurations here if needed
});
