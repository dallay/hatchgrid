import { describe, it } from "vitest";

// TODO: Add tests for workspaceStoreProvider
describe.skip("workspaceStoreProvider", () => {
	it("should provide store correctly", () => {
		// Test implementation needed
	});
});
